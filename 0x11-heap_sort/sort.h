#ifndef _SEARCH_H_
#define _SEARCH_H_

#include <stdio.h>
#include <stdlib.h>
#include <stdint.h>

void print_array(const int *array, size_t size);
void heap_sort(int *array, size_t size);

#endif /* _SEARCH_H_ */
